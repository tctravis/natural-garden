---
const { cardsPerRow } = Astro.props;
---

{cardsPerRow === 2 && <div class={`grid gap-4 auto-rows-auto sm:grid-cols-2`}><slot /></div>}
{cardsPerRow === 3 && <div class={`grid gap-4 auto-rows-auto md:grid-cols-3`}><slot /></div>}
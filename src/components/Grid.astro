---
const { itemsPerRow } = Astro.props;
---

{itemsPerRow === 2 && <div class={`grid gap-4 auto-rows-auto sm:grid-cols-2`}><slot /></div>}
{itemsPerRow === 3 && <div class={`grid gap-4 auto-rows-auto sm:grid-cols-2 md:grid-cols-3`}><slot /></div>}
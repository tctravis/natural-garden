---
import Container from "../components/Container.astro";

const { Element = 'section' } = Astro.props;
---

<Element class={`odd:bg-content-section-odd even:bg-content-section-even py-8`}><Container><slot /></Container></Element>